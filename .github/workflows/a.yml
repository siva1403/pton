name: Bypass Audit Logger

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  audit:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run audit script
        run: |
          python scripts/audit_logger.py \
            "${{ github.event.pull_request.number }}" \
            "${{ github.event.pull_request.merged_by.login }}" \
            "${{ github.event.pull_request.base.ref }}" \
            "${{ github.event.pull_request.title }}"

      - name: Commit audit log
        run: |
          git config user.name "siva1403"
          git config user.email "k.s.krishna1403@gmail.com"

          git add audit/bypass-log.jsonl
          git commit -m "chore: update audit log" || exit 0
          git push
